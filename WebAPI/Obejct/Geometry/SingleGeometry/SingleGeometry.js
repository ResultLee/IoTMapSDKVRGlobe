import GeometryProvider from '../GeometryProvider.js';

class SingleGeometry extends GeometryProvider {
    constructor(positions, options) {
        super(positions, options);
    }
}

export default SingleGeometry;

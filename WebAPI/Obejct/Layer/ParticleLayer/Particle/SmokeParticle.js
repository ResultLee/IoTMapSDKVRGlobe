import ParticleProvider from './ParticleProvider.js';

class SmokeParticle extends ParticleProvider {
    constructor(position, options) {
        super(position, options);
    }
}

export default SmokeParticle;

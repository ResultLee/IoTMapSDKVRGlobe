import GraphicProvider from '../GraphicProvider.js';

class MeasureVerticalGraphic extends GraphicProvider {
    constructor(options) {
        super(options);
    }
}

export default MeasureVerticalGraphic;
